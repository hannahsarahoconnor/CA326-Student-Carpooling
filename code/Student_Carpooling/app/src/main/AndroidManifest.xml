<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student_carpooling">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRTIE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".UserProfile"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UserLocation"
            android:label="@string/title_activity_user_location" />
        <activity android:name=".TripRequests" />
        <activity
            android:name=".RequestMapActivity"
            android:label="@string/title_activity_request_map" />
        <activity
            android:name=".FilteredTrips"
            android:label="@string/title_activity_filtered_trips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DriverMessage"
            android:label="@string/title_activity_driver_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name=".DriverTripItem"
            android:label="@string/title_activity_driver_trip_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TripFilter" />
        <activity
            android:name=".PassengerProfile"
            android:label="@string/title_activity_passenger_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PassengerMessage"
            android:label="@string/title_activity_passenger_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PassengerTrips"
            android:label="@string/title_activity_passenger_trips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FindTrips"
            android:label="@string/title_activity_find_trips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PassengerActivity"
            android:label="@string/title_activity_passenger"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".ResetPassword" />
        <activity
            android:name=".DriverCreate"
            android:label="@string/title_activity_driver_create"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DriverTrips"
            android:label="@string/title_activity_driver_trips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DriverMain"
            android:label="@string/title_activity_driver_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DriverProfile"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>